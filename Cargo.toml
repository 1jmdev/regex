[package]
name = "fast-reg"
version = "0.1.0"
edition = "2024"

[dependencies]
memchr = "2"

[features]
wasm-simd128 = []

[dev-dependencies]
criterion = { version = "0.8.2", default-features = false }
rust-regex = { package = "regex", version = "1" }

[[bench]]
name = "compare"
harness = false
